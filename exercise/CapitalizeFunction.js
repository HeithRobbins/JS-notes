const capital = str => {
    const words = str.split(" ")
    return words.map(word => word[0].toUpperCase() + word.slice(1).join())
}

const String1("come on try it")
console.log(string1)
// console.log(elements.join());
// console.log(str.slice(4, 19));
// console.log(sentence.toUpperCase());